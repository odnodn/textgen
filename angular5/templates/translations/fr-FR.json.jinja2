{
{%  for pck in mdl.elements %}
    //{{  pck.name }}
    {% for entity in genUtils.getEntities(pck) -%}
        {% for prop in entity.properties -%}

            "{{ entity.name }}_{{ prop.name }}": "FR-{{  prop.name | humanize }}",

        {% endfor %}
    {% endfor %}

    {% for entity in genUtils.getEnums(pck) -%}
        {% for prop in entity.properties -%}

            "{{ entity.name }}_{{ prop.name }}": "{{  prop.name | humanize }}",

        {% endfor %}
    {% endfor %}

{% endfor %}
}