
import { Component, OnInit } from '@angular/core';
import { {{name}} } from "../{{lname}}";
import {Router} from "@angular/router";
import { {{name}}Service} from "../{{lname}}.service";
import { BaseListComponent } from '../../base/base-list.component';
import { BaseEntity } from '../../base/base';

@Component({
selector: 'app-{{lname}}-list',
templateUrl: './{{lname}}-list.component.html',
styleUrls: ['./{{lname}}-list.component.css']
})
export class {{name}}ListComponent extends BaseListComponent<{{name}}>
    implements OnInit {

    constructor(protected {{lname}}Service: {{name}}Service, protected router: Router) {
        super({{lname}}Service, router)
    }
}